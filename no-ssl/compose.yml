services:  
  whoami:
    image: traefik/whoami
    restart: unless-stopped
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.${APP_NAME:-whoami-nossl}.rule=Host(`${APP_HOSTNAME:-whoami-nossl.localhost}`)"
      - "traefik.http.routers.${APP_NAME:-whoami-nossl}.entrypoints=web"
      - "traefik.docker.network=platform"
    networks:
      - platform

networks:
  platform:
    external: true
